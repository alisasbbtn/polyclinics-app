document.body.querySelector('.appointment-form select').innerHTML  = "<%= j time_options(@available_hours) %>";
document.body.querySelector('.appointment-form select').disabled = false;
document.body.querySelector('.appointment-form input[type=submit]').disabled = false;